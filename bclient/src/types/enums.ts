export enum AppActions {
  events = 'events',
  items = 'items',
  artists = 'artists',
  news = 'news',
  user = 'user',
}

export enum Term {
  category = 'category',
  genre = 'genre',
  selection = 'selection',
  feeling = 'feeling',
}
