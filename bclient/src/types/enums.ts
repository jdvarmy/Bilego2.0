export enum Cities {
  moscow = 'moscow',
  petersburg = 'petersburg',
}

export enum AppActions {
  events = 'events',
  items = 'items',
  artists = 'artists',
  news = 'news',
  user = 'user',
}

export enum Term {
  category = 'category',
  genre = 'genre',
  selection = 'selection',
  feeling = 'feeling',
}

export enum SortType {
  desc = 'desc',
  asc = 'asc',
  popular = 'popular',
}
